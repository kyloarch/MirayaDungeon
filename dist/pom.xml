<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <groupId>me.kylofz.miraya</groupId>
    <artifactId>mirayadungeon-dist</artifactId>
    <version>${project.parent.version}</version>
    <packaging>jar</packaging>
    <parent>
        <groupId>me.kylofz.miraya</groupId>
        <artifactId>mirayadungeon-parent</artifactId>
        <version>0.19</version>
    </parent>
    <build>
        <finalName>${project.artifactId}-${project.version}${buildNo}</finalName>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <version>3.6.1</version>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>shade</goal>
                        </goals>
                        <configuration>
                            <artifactSet>
                                <includes>
                                    <include>me.kylofz.miraya:*</include>
                                </includes>
                            </artifactSet>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
    <dependencies>
        <dependency>
            <groupId>me.kylofz.miraya</groupId>
            <artifactId>dungeonsxl-adapter</artifactId>
            <version>${project.parent.version}</version>
        </dependency>
        <dependency>
            <groupId>me.kylofz.miraya</groupId>
            <artifactId>mirayadungeon-api</artifactId>
            <version>${project.parent.version}</version>
        </dependency>
        <dependency>
            <groupId>me.kylofz.miraya</groupId>
            <artifactId>dungeonsxl-bukkit_blockdata</artifactId>
            <version>${project.parent.version}</version>
        </dependency>
        <dependency>
            <groupId>me.kylofz.miraya</groupId>
            <artifactId>dungeonsxl-bukkit_magicvalues</artifactId>
            <version>${project.parent.version}</version>
        </dependency>
        <dependency>
            <groupId>me.kylofz.miraya</groupId>
            <artifactId>mirayadungeon-core</artifactId>
            <version>${project.parent.version}</version>
        </dependency>
        <dependency>
            <groupId>me.kylofz.miraya</groupId>
            <artifactId>dungeonsxl-paper</artifactId>
            <version>${project.parent.version}</version>
        </dependency>
        <dependency>
            <groupId>me.kylofz.miraya</groupId>
            <artifactId>dungeonsxl-spigot</artifactId>
            <version>${project.parent.version}</version>
        </dependency>
    </dependencies>
</project>
